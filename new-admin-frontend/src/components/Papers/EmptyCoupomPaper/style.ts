export default (theme: any) => ({
    title: {
        fontSize: 34,
        fontWeight: 'bold',
        marginBottom: theme.spacing(2)
    },
    description: {
        fontSize: 16,
        marginBottom: theme.spacing(2)
    }
}) as any