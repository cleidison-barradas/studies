import { createStyles, Theme } from '@material-ui/core'

const styles = (theme: Theme) =>
  createStyles({
    divtexthelp: {
      display: 'flex',
      alignItems: 'center',
    },
    iconhelp: {
      fontSize: 16,
      color: '#B8C5D0',
      marginLeft: 5,
    },
  })

export default styles
