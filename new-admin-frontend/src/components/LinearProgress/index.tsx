import { LinearProgressProps } from './model'
import { LinearProgressStyled } from './styles'

export const LinearProgress = (props: LinearProgressProps) => (
  <LinearProgressStyled {...props}>
    <div />
  </LinearProgressStyled>
)
