import styled from 'styled-components'

export const TecInfoCard = styled.div`
  border: 1px solid ${({ theme }) => theme.color.neutral.light};
  border-radius: 24px;
  background: white;
  padding: 16px;
  padding-top: 8px;
`
